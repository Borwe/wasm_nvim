[package]
name = "xtask"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }

[target.'cfg(windows)'.dependencies]
reqwest = { version = "0.11", features = ["json"] }
zip = "0.6"
cc = "1.0"
